---
import Share from "@/components/document/Share.astro";

interface Props {
  shareTitle: string;
  source: string;
}

const { shareTitle, source } = Astro.props;
---

<Share title={shareTitle} url={Astro.url.toString()} source={source} />
