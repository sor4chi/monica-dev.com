{
  "name": "website",
  "type": "module",
  "scripts": {
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "oxfmt --write .",
    "format:check": "oxfmt --check .",
    "fix": "pnpm lint:fix && pnpm format",
    "dev:portfolio": "pnpm run --filter portfolio dev",
    "dev:dashboard": "pnpm run --filter @sor4chi/dashboard dev"
  },
  "devDependencies": {
    "oxlint": "^1.59.0",
    "oxfmt": "^0.44.0",
    "typescript": "^5.9.3"
  }
}
